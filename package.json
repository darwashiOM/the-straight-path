{
  "name": "the-straight-path",
  "version": "0.1.0",
  "private": true,
  "description": "The Straight Path — a clear, pastoral introduction to Islam.",
  "license": "MIT",
  "author": "darwashiOM",
  "repository": {
    "type": "git",
    "url": "https://github.com/darwashiOM/the-straight-path.git"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "pnpm --filter @tsp/web dev",
    "prebuild": "node scripts/generate-reading-times.mjs && node scripts/generate-sitemap.mjs && node scripts/generate-llms-full.mjs",
    "build": "pnpm --filter @tsp/web build",
    "preview": "pnpm --filter @tsp/web preview",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,mdx,css,html,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,mdx,css,html,yml,yaml}\"",
    "emulators": "firebase emulators:start",
    "deploy": "pnpm build && firebase deploy",
    "deploy:hosting": "pnpm build && firebase deploy --only hosting",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:rules": "firebase deploy --only firestore:rules,storage:rules",
    "size": "size-limit",
    "size:why": "size-limit --why"
  },
  "devDependencies": {
    "@size-limit/file": "^12.1.0",
    "firebase-admin": "^12.7.0",
    "prettier": "^3.3.3",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "size-limit": "^12.1.0"
  }
}
